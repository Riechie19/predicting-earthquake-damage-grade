# predicting-earthquake-damage-grade
This project predicts building damage grades from the 2015 Gorkha earthquake using machine learning models like LightGBM, Random Forest, and KNN. Data preprocessing includes handling outliers, missing values, and feature selection. LightGBM, trained on label-encoded data, outperformed other models in predicting damage severity.
